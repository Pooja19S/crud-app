pipeline:
  identifier: Build_Pooja19S_crud_app_1723181338318
  name: Build crud-app
  orgIdentifier: default
  projectIdentifier: NodejsCrudapp
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: account.Github
        repoName: Pooja19S/crud-app
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          caching:
            enabled: true
          cloneCodebase: true
          execution:
            steps:
              - step:
                  identifier: installdependencies
                  name: install dependencies
                  spec:
                    command: npm install
                  timeout: ""
                  type: Run
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
        type: CI
